VCC_PORT=5000
BACKEND_URL=http://localhost:4000

# Stripe Issuing (test mode) — issues a unique virtual card per order.
# Get your test key from: dashboard.stripe.com → Developers → API keys
# Must start with sk_test_ for devnet use.
# Leave empty to fall back to hardcoded test cards (no Stripe account needed).
# STRIPE_SECRET_KEY=sk_test_...
